{
  "name": "panierr-examples",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "parcel  src/index.html",
    "start": "npm run build && npx live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@stripe/stripe-js": "^4.4.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "parcel": "^2.12.0",
    "typescript": "^5.6.2"
  }
}